# Python 3.9+
# The analysis layer (build_tradability_panel / audit_trades / validate_audit) is
# offline and deterministic. Real-data acquisition is delegated to the sibling
# skill-pandadata-api and is NOT required to run the audit.
pandas>=1.3
numpy>=1.20
# optional: parquet I/O for large panels
# pyarrow>=8.0
